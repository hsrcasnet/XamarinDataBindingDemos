<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="DataBindingDemo.Views.LoginPage"
    Title="Welcome to Xamarin Forms"
    Padding="20">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Sign Up" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout VerticalOptions="Center">
            <Label Text="Username" />
            <Entry Placeholder="Username" Text="{Binding Username, Mode=TwoWay}" />
            <Label Text="Password" />
            <Entry
                Placeholder="Password"
                Text="{Binding Password, Mode=TwoWay}"
                IsPassword="true" />
            <Label Text="Accept Terms and Conditions?" />
            <CheckBox x:Name="CheckBoxTermsAndConditions" IsChecked="{Binding AcceptTermsAndConditions, Mode=TwoWay}" />
            <Button Text="Login" IsEnabled="{Binding AcceptTermsAndConditions}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>